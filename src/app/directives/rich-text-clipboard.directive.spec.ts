import { RichTextClipboardDirective } from './rich-text-clipboard.directive';

describe('RichTextClipboardDirective', () => {
  it('should create an instance', () => {
    const directive = new RichTextClipboardDirective();
    expect(directive).toBeTruthy();
  });
});
